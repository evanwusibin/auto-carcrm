# Auto-CarCRM 项目改进清单

> 📅 生成日期：2026-06-28
> 🎯 目的：明确项目与企业级标准的差距，给出分步改进指导
> 📊 评估基准：企业 AI 应用工程师岗位要求

---

## 一、项目现状评估

### 做得好的（优势）

| 维度 | 评价 | 企业对标 |
|------|------|----------|
| 架构分层 | 4 层分离（API→Page→Graph→RAG Service→Infra） | 符合 Clean Architecture |
| 检索设计 | 5 路并行 + RRF + 交叉编码器 Reranker | 超过多数初级项目 |
| 意图分流 | 闲聊/投诉跳过检索，省算力 | 企业级成本控制 |
| 置信度兜底 | 低于 0.6 反问，不硬答 | 企业级可信度 |
| SSE 流式 | 完整的流式输出链路 | 标准实践 |
| Prompt 工程 | 13 个结构化模板 | 质量较高 |

### 需要改进的（差距）

共 14 项，按严重程度分为三级：
- 🔴 Critical（安全/部署红线，必须修）
- 🟡 Important（工程质量，应该修）
- 🟢 Nice-to-have（锦上添花，有空修）

---

## 二、逐项详细说明

---

### 🔴 1. 应用无 Dockerfile

#### 缺什么

项目只有 Milvus 的 `docker-compose.milvus.yml`，应用本身没有 Dockerfile，无法容器化部署。

#### 为什么缺

学习阶段用 `python app/main.py` 直接跑即可，容器化属于运维/后端范畴，课程未覆盖。

#### 好坏对比

```text
当前（无 Docker）：
  ✓ 启动快，开发方便
  ✗ 换机器要重新装依赖
  ✗ 无法保证环境一致性
  ✗ 无法水平扩展
  ✗ 面试时说不出部署方案

Docker 化后：
  ✓ 一条命令启动全套环境
  ✓ 开发/测试/生产环境一致
  ✓ 可以对接 K8s 做自动扩缩容
  ✓ 面试时能说清部署架构
  ✗ 需要学习 Dockerfile 编写
```

#### 对企业的影响

```text
- 无法快速部署到新服务器
- 无法做灰度发布
- 无法做多实例负载均衡
- 运维成本高（手动装依赖容易出错）
```

#### 对个人的影响

```text
- 面试被问"你的项目怎么部署的"答不上来
- 缺少 DevOps 基本能力
- 无法独立完成从开发到上线的闭环
```

#### 分步指导

```text
Step 1: 创建 Dockerfile
  - 基础镜像：python:3.11-slim
  - 安装依赖：COPY requirements.txt → pip install
  - 复制代码：COPY app/ /app/
  - 暴露端口：EXPOSE 8000
  - 启动命令：CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

Step 2: 创建 docker-compose.yml（完整版）
  - app（你的 FastAPI 应用）
  - milvus-standalone（向量数据库）
  - etcd（Milvus 依赖）
  - minio（对象存储）
  - mongodb（业务数据库）
  - 配置 network 让所有容器互通

Step 3: 创建 .dockerignore
  - 排除 __pycache__、.git、model/（大文件不进镜像）

Step 4: 本地验证
  - docker-compose up --build
  - 访问 http://localhost:8000/docs 验证 API
```

---

### 🔴 2. 认证鉴权存在安全漏洞

#### 缺什么

`auth.py` 中 `get_current_user` 在无 token 时 fallback 到 `debug-user`，等于认证形同虚设。`auth.py` 还硬编码了 `MONGO_URL` 和 `MONGO_DB`，绕过了统一配置。

#### 为什么缺

学习阶段为了方便调试，加了 debug fallback。课程未强调安全最佳实践。

#### 好坏对比

```text
当前（debug-user fallback）：
  ✓ 调试方便，不用每次传 token
  ✗ 任何人都能以管理员身份访问
  ✗ 无法追踪操作人
  ✗ 企业安全审计直接不过

修复后：
  ✓ 无 token 时返回 401，强制登录
  ✓ 所有操作可追溯到用户
  ✓ 符合企业安全规范
  ✗ 调试时需要手动传 token
```

#### 对企业的影响

```text
- 数据泄露风险（任何人可读写知识库）
- 合规风险（无法审计谁做了什么操作）
- 责任追溯困难（出问题找不到人）
```

#### 对个人的影响

```text
- 面试被问"你的项目怎么保证安全的"答不上来
- 缺少安全意识是 AI 应用岗的大忌
- 真实工作中安全漏洞可能导致严重后果
```

#### 分步指导

```text
Step 1: 删除 debug-user fallback
  - 无 token 或 token 无效时，直接返回 HTTP 401
  - 去掉 get_current_user 中的 try-except fallback

Step 2: 统一配置管理
  - 删除 auth.py 中硬编码的 MONGO_URL/MONGO_DB
  - 改用 settings_config 统一读取

Step 3: 加入角色权限（RBAC）
  - 定义角色：admin / editor / viewer
  - 知识库导入只允许 admin/editor
  - 查询允许所有角色

Step 4: 加入操作审计日志
  - 记录谁在什么时间做了什么操作
  - 存入 MongoDB 的 audit_logs 集合
```

---

### 🔴 3. 输入校验缺失（注入风险）

#### 缺什么

chat 端点接收 `dict[str, Any]`，没有 Pydantic 模型校验。Milvus 过滤表达式使用 f-string 拼接，存在注入风险。

#### 为什么缺

学习阶段信任前端输入，未考虑恶意请求。

#### 好坏对比

```text
当前（无校验）：
  ✓ 代码简单，不用定义模型
  ✗ 恶意输入可能导致 MongoDB/Milvus 注入
  ✗ 无效输入浪费 LLM 算力
  ✗ 接口文档不清晰（不知道该传什么）

修复后：
  ✓ Pydantic 模型定义清楚的请求格式
  ✓ 自动参数校验（类型、长度、必填）
  ✓ Milvus 过滤表达式使用参数化拼接
  ✓ 自动生成 OpenAPI 文档
```

#### 分步指导

```text
Step 1: 定义 ChatRequest 模型
  class ChatRequest(BaseModel):
      query: str = Field(..., min_length=1, max_length=500)
      session_id: str | None = None
      # ... 其他字段

Step 2: 替换 chat 端点的参数类型
  原来：async def chat(body: dict)
  改为：async def chat(body: ChatRequest)

Step 3: Milvus 过滤表达式参数化
  原来：f"item_name in {item_names}"
  改为：使用 Milvus SDK 的参数化查询或白名单校验
```

---

### 🟡 4. 无限流保护

#### 缺什么

无 rate limit 中间件，`rate_limit_utils.py` 存在但未接入。

#### 为什么缺

学习阶段单用户测试，不需要限流。

#### 好坏对比

```text
当前（无限流）：
  ✓ 简单
  ✗ 恶意用户可以刷爆 LLM API
  ✗ 服务器资源被耗尽

限流后：
  ✓ 每用户每分钟最多 N 次请求
  ✓ 保护 LLM API 调用成本
  ✓ 防止 DDoS
```

#### 分步指导

```text
Step 1: 接入 rate_limit_utils.py（已有代码）
  在 FastAPI middleware 中注册

Step 2: 配置限流规则
  - 匿名用户：10 次/分钟
  - 登录用户：60 次/分钟
  - 管理员：无限制

Step 3: 超限时返回 HTTP 429
```

---

### 🟡 5. 无异步执行

#### 缺什么

LangGraph `invoke()` 是阻塞调用，整个查询管道在请求线程中同步执行。

#### 为什么缺

LangGraph 默认是同步的，异步需要额外配置 `async invoke`。

#### 对比

```text
当前（同步）：
  - 10 个并发请求 = 10 个线程阻塞
  - 吞吐量受限于线程池大小

异步后：
  - 10 个并发请求共享事件循环
  - I/O 等待（LLM 调用、Milvus 查询）不阻塞其他请求
  - 吞吐量提升 5-10 倍
```

#### 分步指导

```text
Step 1: 将 graph node 改为 async def
Step 2: 使用 graph.ainvoke() 替代 invoke()
Step 3: LLM 调用改用 acompletion（LangChain async）
Step 4: Milvus 查询改用异步客户端
```

---

### 🟡 6. 无可观测性

#### 缺什么

只有 `loguru` 日志，无分布式追踪（OpenTelemetry）、无指标（Prometheus）、无告警。

#### 为什么缺

学习阶段看控制台日志就够了，监控属于运维范畴。

#### 对比

```text
当前（只有日志）：
  ✓ 能看到基本信息
  ✗ 无法知道哪个节点慢
  ✗ 无法知道 LLM 调用成功率
  ✗ 出问题只能翻日志

加监控后：
  ✓ Grafana 仪表盘实时查看 QPS/延迟/错误率
  ✓ 链路追踪定位慢节点
  ✓ 异常自动告警（钉钉/邮件）
```

#### 分步指导

```text
Step 1: 接入 OpenTelemetry
  - 安装 opentelemetry-sdk + opentelemetry-exporter-otlp
  - 在 FastAPI 中间件注入 trace

Step 2: 关键节点埋点
  - query_graph 每个 node 的耗时
  - LLM 调用的 latency 和 token 用量
  - Milvus 查询的耗时和返回条数

Step 3: 接入 Prometheus + Grafana
  - 暴露 /metrics 端点
  - 配置 Grafana Dashboard
```

---

### 🟡 7. 无缓存层

#### 缺什么

无 Redis 缓存，相同查询每次都重新走完整 RAG 管道。

#### 对比

```text
当前：
  用户问"比亚迪T5保养周期" → 完整检索+生成
  用户再问同样问题 → 又完整检索+生成（浪费）

加缓存后：
  首次查询 → 完整管道 → 结果存 Redis（TTL 1小时）
  重复查询 → 直接返回缓存 → 省 LLM 算力
```

#### 分步指导

```text
Step 1: 部署 Redis
Step 2: 查询前查缓存（key = query 的 hash）
Step 3: 结果写入缓存（TTL 根据业务设定）
Step 4: 知识库更新时清缓存
```

---

### 🟢 8. 无错误恢复/检查点

#### 缺什么

图执行中某个节点失败，整个查询失败。LangGraph 支持 checkpoint 但未使用。

#### 分步指导

```text
Step 1: 配置 MemorySaver 或 SQLite checkpointer
Step 2: graph.invoke() 加 config={"configurable": {"thread_id": session_id}}
Step 3: 失败时从最近的 checkpoint 恢复
```

---

### 🟢 9. 无评估体系

#### 缺什么

无 RAGAS/TruLens 评估，不知道 RAG 质量到底好不好。

#### 分步指导

```text
Step 1: 准备评估数据集（50-100 条 QA 对）
Step 2: 接入 RAGAS 框架
Step 3: 评估指标：Faithfulness / Answer Relevancy / Context Recall
Step 4: 每次知识库更新后自动跑评估
```

---

### 🟢 10. 无多租户支持

#### 缺什么

单知识库，多部门使用时数据会互相污染。

#### 分步指导

```text
Step 1: MongoDB 加 tenant_id 字段
Step 2: Milvus collection 按租户隔离或加租户过滤
Step 3: API 层根据用户身份自动注入 tenant_id
```

---

## 三、改进优先级排序

### 第一批：面试前必须改（1-2 天）

```text
1. 创建 Dockerfile + docker-compose.yml
2. 修复认证漏洞（去掉 debug-user）
3. 加输入校验（Pydantic 模型）
4. 接入已有的 rate_limit_utils
```

### 第二批：简历上可以写（3-5 天）

```text
5. LangGraph 异步化
6. 接入 Redis 缓存
7. 加 OpenTelemetry 链路追踪
8. 写评估脚本（RAGAS）
```

### 第三批：有时间再搞（1-2 周）

```text
9. LangGraph checkpoint 错误恢复
10. 多租户支持
11. CI/CD（GitHub Actions）
12. Prometheus + Grafana 监控
```

---

## 四、面试话术

### Q: 你的 RAG 项目有什么亮点？

> "我设计了 5 路并行检索——向量检索、关键词检索、结构化查询、维修案例检索、HyDE 检索——用 RRF 融合后经过交叉编码器 Reranker 重排序。还有意图识别分流，闲聊和投诉跳过检索直接回答，节省算力。置信度低于 0.6 时反问用户，不硬答。"

### Q: 你的项目有什么不足？

> "目前缺少容器化部署（Dockerfile 还没写），认证鉴权有 debug fallback 需要修复，没有加缓存层和监控。这些是我接下来要补的。"

### Q: 你打算怎么改进？

> "分三步：先 Docker 化 + 修复安全漏洞，再加 Redis 缓存 + 链路追踪，最后接 RAGAS 评估体系。目标是让它达到能上线的标准。"

---

📁 **输出位置**：`D:\heimaAI\PytorchSDXX\08_掌柜智库\实战\实战\auto-carcrm\项目改进清单.md`
