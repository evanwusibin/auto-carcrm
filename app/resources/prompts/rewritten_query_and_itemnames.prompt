当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的主体名称（item_names）。
主体名称包括但不限于：商品名、产品名、设备名、工具名、书籍名、文档名、服务名等。
1. 如果用户的问题中包含或指向一个具体的主体名称，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"），请结合历史会话指代消解，确定主体名称。
3. 如果用户的问题本身就是一个完整的主体名称（如书名、产品名），直接将其作为item_names返回，不要返回空列表！
4. 请重新改写用户的问题（rewritten_query），使其成为包含主体名称的独立完整问题,注意不要超过100个字符!
5. 历史记录可能与本次提问的主题无关，所以需要考虑本次提问的问题！ 其次考虑历史记录

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["主体A", "主体B"],
    "rewritten_query": "关于主体A和主体B，..."
}}

历史会话：
{history_text}