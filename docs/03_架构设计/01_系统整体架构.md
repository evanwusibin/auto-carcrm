# 系统整体架构设计

> 文档编号：DOC-03-01 | 版本：v1.0 | 更新时间：2026-06-12

---

## 一、架构总览

系统架构分为 **五层**：数据源层、知识处理层（离线）、检索增强层（在线）、生成服务层、应用交互层。

```
┌─────────────────────────────────────────────────────────┐
│                     应用交互层                            │
│   C端（终端客户）   B端（经销商）   管理端（厂家）           │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                     生成服务层                            │
│   FastAPI 接口服务   LLM 调用   Prompt 管理   答案校验     │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   检索增强层（在线）                       │
│  意图识别  实体抽取  多路混合检索  Rerank  元数据过滤       │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   知识处理层（离线）                       │
│  文档解析  文本清洗  Chunk切分  Embedding  向量索引构建     │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                      数据源层                             │
│  保养手册  质保手册  维修手册  典型案例  车辆档案  保养记录  │
└─────────────────────────────────────────────────────────┘
```

---

## 二、核心链路说明

### 2.1 知识入库链路（离线）

```
原始文档上传
    ↓
文件格式识别（PDF / Word / Excel / 图片扫描件）
    ↓
内容解析（MinerU）
    ↓
文本清洗（去页眉页脚 / 去噪 / 合并断行 / 表格转文本）
    ↓
结构识别（章节标题 / 故障码 / 保养里程 / 质保期限）
    ↓
Chunk 切分（按章节 / FAQ对 / 案例 / 固定长度兜底）
    ↓
元数据绑定（车型 / 部件 / 版本 / 生效时间 / 权限）
    ↓
Embedding 向量化（bge-large-zh / bge-m3）
    ↓
写入 MongoDB（文本 + 向量 + 元数据）
    ↓
建立向量索引（Milvus 混合搜索）
    ↓
审核 → 发布 → 可被检索
```

### 2.2 在线问答链路

```
用户自然语言提问
    ↓
问题预处理（清洗 / 上下文补全 / 多轮改写）
    ↓
意图识别（保养 / 质保 / 故障 / 案例 / 流程 / 报修）
    ↓
实体抽取（车型 / VIN / 故障码 / 里程 / 部件）
    ↓
多路混合检索（四路并行 + 可选旁路）
    ├── [路由1] 向量语义检索（Milvus 混合搜索）
    ├── [路由2] 关键词精确检索（BM25）
    ├── [路由3] 结构化业务查询（车辆档案 / 保养记录 / 质保规则）
    ├── [路由4] 历史案例检索
    ├── [可选] HyDE 检索（假设性答案，RAG_ENABLE_HYDE 控制）
    └── [可选] Web 搜索（联网补充，RAG_ENABLE_WEB 控制）
    ↓
元数据过滤（车型 / 版本 / 有效期 / 权限）
    ↓
结果融合与去重
    ↓
Rerank 重排序
    ↓
置信度判断
    ├── 足够 → Prompt 组装 → LLM 生成答案 → 引用溯源
    └── 不足 → 追问补充信息 / 无答案提示 / 转人工
    ↓
答案校验（防幻觉 / 风险提示 / 来源绑定）
    ↓
返回答案 + 引用来源
    ↓
日志记录
```

---

## 三、技术组件架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端                                   │
│  Web / App / 微信小程序                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP / WebSocket
┌───────────────────────────▼─────────────────────────────────────┐
│                       FastAPI 服务层                              │
│  /api/knowledge/*   /api/qa/*   /api/repair/*   /api/vehicle/*   │
└──┬──────────┬──────────┬──────────┬────────────┬────────────────┘
   │          │          │          │            │
   ▼          ▼          ▼          ▼            ▼
知识处理    检索服务    LLM服务    业务服务     日志服务
   │          │          │          │            │
   ▼          ▼          ▼          ▼            ▼
文档解析   向量检索   mimo-      MongoDB      MongoDB
切分向量   BM25检索   v2.5-pro   业务集合     日志集合
   │       Rerank      调用
   ▼          │
MongoDB    MongoDB
知识集合   Vector
向量索引   Search
```

---

## 四、多路混合检索架构

这是本系统的核心技术亮点，采用**四路并行召回 + 可选旁路 + 融合重排**的策略。

### 4.1 检索流程图

```
用户问题
    │
    ├──→ [路由1] 向量语义检索（必选）
    │         └─ BGE-M3 Embedding → Milvus 混合搜索（稠密+稀疏）→ Top-K 语义相关 Chunk
    │
    ├──→ [路由2] 关键词精确检索（必选）
    │         └─ BM25 + jieba 分词 → 故障码 / 车型 / 部件 / 政策编号 精确匹配
    │
    ├──→ [路由3] 结构化业务查询（必选）
    │         └─ 车辆档案 / 保养记录 / 质保规则 → MongoDB 结构化查询
    │
    ├──→ [路由4] 历史案例检索（必选）
    │         └─ 故障现象向量相似度 → 典型案例集合 → 相似案例 Top-K
    │
    ├──→ [可选] HyDE 检索（RAG_ENABLE_HYDE=true 时启用）
    │         └─ LLM 生成假设性答案 → 用答案向量化检索 → 补充语义召回
    │
    └──→ [可选] Web 搜索（RAG_ENABLE_WEB=true 时启用）
              └─ MCP 调用百炼联网搜索 → 补充外部知识（如最新政策、天气等）

各路结果 → 合并去重 → 元数据过滤（车型/版本/有效期/权限）→ Rerank 重排序 → Top-5 上下文
```

### 4.2 四路检索的价值

| 检索路径 | 解决的问题 | 示例 | 必选/可选 |
|---|---|---|---|
| 向量语义 | 口语化描述、模糊表达 | "车子突然没劲" → 动力下降故障 | 必选 |
| 关键词精确 | 专业术语、编号 | "P0A0F" → 精确故障码释义 | 必选 |
| 结构化查询 | 个性化判断 | "我这辆车还在保吗" → 查车辆档案+质保规则 | 必选 |
| 案例检索 | 经验复用 | "有没有类似案例" → 相似维修案例 | 必选 |
| HyDE检索 | 补充语义召回 | 复杂问题生成假设性答案再检索 | 可选 |
| Web搜索 | 补充外部知识 | 最新政策、天气、新闻等 | 可选 |

### 4.3 可选旁路说明

**HyDE 检索（默认关闭）**
- **作用**：先让 LLM 生成假设性答案，用答案去检索，补充语义召回
- **适用场景**：复杂问题、多步推理问题
- **配置**：`RAG_ENABLE_HYDE=true` 启用
- **性能影响**：增加 1-2 秒延迟（需要调用 LLM）

**Web 搜索（默认关闭）**
- **作用**：通过 MCP 调用百炼联网搜索，补充外部知识
- **适用场景**：最新政策、天气、新闻等时效性信息
- **配置**：`RAG_ENABLE_WEB=true` 启用
- **性能影响**：增加 2-3 秒延迟（需要调用外部 API）
- **注意事项**：
  - 企业内部机密信息（维修手册、质保政策）不应发到外部搜索
  - 外部搜索结果不可控，需要额外的可信度评估
  - 建议仅在知识库覆盖不足时启用

### 4.4 配置示例

```env
# .env 配置
RAG_VECTOR_TOPK=5           # 向量检索 Top-K
RAG_KEYWORD_TOPK=5          # 关键词检索 Top-K
RAG_FINAL_TOPK=5            # 最终返回 Top-K
RAG_CONFIDENCE_THRESHOLD=0.7 # 置信度阈值
RAG_RRF_K=60                # RRF 融合参数
RAG_ENABLE_HYDE=false       # 是否启用 HyDE（默认关闭）
RAG_ENABLE_WEB=false        # 是否启用 Web 搜索（默认关闭）
```

---

## 五、Prompt 设计策略

```python
SYSTEM_PROMPT = """
你是比亚迪商用车售后智能助手，负责基于企业内部知识库回答售后服务相关问题。

核心约束：
1. 只能基于【参考资料】中的内容回答，不得编造政策、价格、配置或流程
2. 参考资料不足时，明确告知"当前资料中未找到足够依据"
3. 涉及质保结论时，表达为"初步预判"，提示以服务站检测为准
4. 涉及安全风险时，必须在答案中突出提示
5. 回答先给结论，再补充依据，结构清晰

输出格式：
- 直接结论（1-2句）
- 依据说明（来自参考资料）
- 注意事项（如有安全风险或需人工确认）
"""

USER_PROMPT_TEMPLATE = """
用户问题：{question}

车辆信息：
- 车型：{vehicle_model}
- VIN：{vin}
- 当前里程：{mileage}
- 购车日期：{purchase_date}

参考资料：
{context}

请回答：
"""
```

---

## 六、质保预判逻辑设计

质保预判是"RAG + 规则引擎"的结合，不能单纯依赖 LLM 判断。

```python
def warranty_precheck(vehicle, component, fault_description):
    """
    质保预判逻辑：
    1. 查询车辆档案（购车日期、里程）
    2. 查询质保规则（年限、里程、适用部件）
    3. 查询保养记录（是否按时保养）
    4. 检查免责条件（事故、改装、泡水等）
    5. 综合判断并输出预判结果
    """
    rules = get_warranty_rules(vehicle.model, component)
    
    time_ok = check_time_warranty(vehicle.purchase_date, rules.years)
    mileage_ok = check_mileage_warranty(vehicle.mileage, rules.mileage_limit)
    maintenance_ok = check_maintenance_records(vehicle.id)
    exclusion_risk = check_exclusion_keywords(fault_description, rules.exclusions)
    
    if not time_ok or not mileage_ok:
        return PreCheckResult.LIKELY_OUT_OF_WARRANTY
    elif not maintenance_ok:
        return PreCheckResult.MANUAL_REVIEW_REQUIRED
    elif exclusion_risk:
        return PreCheckResult.MANUAL_REVIEW_REQUIRED
    else:
        return PreCheckResult.LIKELY_IN_WARRANTY
```

---

## 七、系统非功能需求

| 指标 | 目标值 | 说明 |
|---|---|---|
| 问答响应时间 | < 5秒 | 含检索 + LLM 生成 |
| 文档导入时间 | < 2分钟/份 | 50页以内文档 |
| 向量检索延迟 | < 200ms | Top-10 召回 |
| 系统可用性 | > 99.5% | 月度统计 |
| 并发用户数 | > 100 | 同时在线 |
| 数据安全 | 接口鉴权 + HTTPS | JWT Token |
| 幻觉控制 | 引用来源覆盖率 > 90% | 答案必须有资料依据 |

---

*文档版本：v1.0 | 更新时间：2026-06-12*
