# 开发待办清单

> 更新时间：2026-06-14

## 一、搜索页面（frontend/search.html）

- [x] 搜索页面流程查询框流程描述改为"主体确认→意图识别→六路混合检索→RRF融合→ReRank精排"
- [x] 搜索页面流程图节点调整为"主体确认→意图+实体→六路召回→RRF融合→ReRank精排→答案生成"
- [x] 搜索页面流式输出进度条改为6步（主体确认→意图识别→六路召回→RRF融合→ReRank精排→答案生成）
- [x] 搜索页面顶部检索详情说明更新为新的6步流程
- [x] 搜索页面顶部标签栏描述改为"主体确认→六路混合检索→RRF融合→ReRank精排·引用溯源"
- [x] 搜索页面问答首页6个快捷选项点击调用真实API（不再用假数据）

## 二、会话管理

- [x] 会话列表显示历史会话，基于MongoDB聚合查询，不再硬编码
- [x] 会话切换支持加载历史消息
- [x] 会话列表支持新增会话
- [x] 会话列表支持删除会话（鼠标悬浮显示×按钮，点击确认后删除）
- [x] 会话消息数量和相对时间显示

## 三、后端API

- [x] 新增GET /api/v1/chat/sessions 接口（获取会话列表）
- [x] 新增DELETE /api/v1/chat/history/{session_id} 接口（删除会话）
- [x] 新增POST /api/v1/feedback/submit 接口（反馈提交）

## 四、流程文档

- [x] 更新 app/rag/query/README.txt 新增会话管理和反馈存储章节
- [x] 更新 flows/02_RAG检索流程/01_多路混合检索流程.md 新增会话管理时序图
- [x] 更新流程图版本号至v1.4

## 五、数据导入

- [ ] 通过import.html页面导入doc目录下的知识文档（商用车三包政策与保养手册.md、新能源商用车故障码大全.md、比亚迪商用车维修案例库.md、三包手册.pdf）
- [ ] 确认Milvus中已有向量数据
- [ ] 确认MongoDB知识文档库已有数据

## 六、待修复问题

- [ ] web_search_service.py MCP连接超时导致整个LangGraph管道阻塞（需添加超时机制）
- [ ] intent_recognition_service.py load_prompt参数不匹配问题（已修复）
- [ ] entity_extraction_service.py load_prompt参数不匹配问题（已修复）
- [ ] 前端API响应code判断问题（success_response默认code=0，前端误判为失败，已修复）
- [ ] SSE流式订阅改用addEventListener监听事件（已修复）

## 七、测试验证

- [ ] 重启服务后测试非流式查询
- [ ] 重启服务后测试流式查询
- [ ] 测试候选主体按钮点击重查
- [ ] 测试反馈按钮（有用/无用）提交
- [ ] 测试会话切换和历史消息加载
- [ ] 测试会话删除功能
