# 多路混合检索流程

> 流程编号：FLOW-02-01 | 版本：v1.1 | 更新时间：2026-06-12

**流程说明**：本流程为在线检索核心链路。采用四路并行召回策略，解决单一向量检索对精确关键词不敏感、对结构化业务数据无法覆盖的问题。

---

## 完整多路混合检索流程图

```mermaid
flowchart TD
    %% ====== 三层调用链（外 → 内）======
    HTTP[HTTP 请求<br/>POST /api/v1/chat/query] --> R-QA[路由层 R-QA<br/>app/api/routers/chat.py]
    R-QA --> P-QA[页面层 P-QA<br/>app/process/query/page/query_page.py]
    P-QA --> G-QA[图编排层 G-QA<br/>query_graph_app.invoke]
    G-QA --> A[用户输入售后问题]
    A --> B[问题预处理]

    B --> B1[去噪/纠错]
    B --> B2[多轮上下文补全 结合历史会话改写]
    B --> B3[查询扩展 口语→专业术语]

    B1 --> C[意图识别]
    B2 --> C
    B3 --> C

    C --> C1{问题意图分类}
    C1 -- 保养咨询 --> CA[意图: maintenance_query]
    C1 -- 质保咨询 --> CB[意图: warranty_query]
    C1 -- 故障诊断 --> CC[意图: fault_diagnosis]
    C1 -- 案例查询 --> CD[意图: case_search]
    C1 -- 报修流程 --> CE[意图: repair_process]
    C1 -- 普通问答 --> CF[意图: general_qa]

    CA --> D[实体抽取 NER]
    CB --> D
    CC --> D
    CD --> D
    CE --> D
    CF --> D

    D --> D1[🚗 车型 T5/T7/轻卡/重卡]
    D --> D2[🔑 VIN/车牌]
    D --> D3[⚠️ 故障码 P0A0F/P1D00]
    D --> D4[🔧 部件 电池/电机/制动]
    D --> D5[📏 里程 6万公里/60000km]
    D --> D6[📅 时间 购车日期/保养日期]
    D --> D7[💬 故障现象 无法启动/异响]

    D1 --> E[构建检索条件]
    D2 --> E
    D3 --> E
    D4 --> E
    D5 --> E
    D6 --> E
    D7 --> E

    E --> F[🔀 四路并行检索]

    F --> F1[路由1️⃣: 向量语义检索 Milvus 混合搜索 Embedding 相似度 Top-20]
    F --> F2[路由2️⃣: 关键词精确检索 BM25 故障码/车型/部件精确匹配]
    F --> F3[路由3️⃣: 结构化业务查询 MongoDB 查询 车辆档案/保养记录/质保规则]
    F --> F4[路由4️⃣: 历史案例检索 典型案例集合 故障现象向量相似]

    F1 --> G[候选结果合并]
    F2 --> G
    F3 --> G
    F4 --> G

    G --> H[去重处理 同一 chunk_id 保留最高分]

    H --> I[元数据过滤]
    I --> I1[✅ 车型匹配过滤 vehicle_model 匹配]
    I --> I2[✅ 有效期过滤 expire_date >= 今天]
    I --> I3[✅ 版本优先 优先最新版本]
    I --> I4[✅ 权限过滤 visible_roles 包含用户角色]
    I --> I5[✅ 状态过滤 chunk.state = active]

    I1 --> J[Rerank 重排序]
    I2 --> J
    I3 --> J
    I4 --> J
    I5 --> J

    J --> K[Cross-Encoder 重排 计算问题-片段相关性]

    K --> L{置信度判断}
    L -- Top-1 分数 >= 阈值 --> M[✅ 选取 Top-5 上下文]
    L -- Top-1 分数 < 阈值 --> N[⚠️ 触发低置信度处理]

    N --> N1{原因分析}
    N1 -- 缺少车型 --> NA[追问: 请告诉我您的车型]
    N1 -- 缺少里程 --> NB[追问: 请告诉我当前行驶里程]
    N1 -- 缺少故障码 --> NC[追问: 仪表盘是否显示故障码]
    N1 -- 知识库无资料 --> ND[提示: 当前资料中未找到依据 建议联系服务站]
    N1 -- 权限不足 --> NE[提示: 无权查看该内容]

    M --> O[进入 RAG 生成流程]
```

---

## 四路检索说明

### 可选旁路（默认关闭）

**HyDE 检索**（RAG_ENABLE_HYDE=true）
- **作用**：先让 LLM 生成假设性答案，用答案去检索，补充语义召回
- **适用场景**：复杂问题、多步推理问题
- **性能影响**：增加 1-2 秒延迟（需要调用 LLM）

**Web 搜索**（RAG_ENABLE_WEB=true）
- **作用**：通过 MCP 调用百炼联网搜索，补充外部知识
- **适用场景**：最新政策、天气、新闻等时效性信息
- **性能影响**：增加 2-3 秒延迟（需要调用外部 API）
- **注意事项**：企业内部机密信息不应发到外部搜索

### 路由1：向量语义检索

**适用场景**：用户描述口语化、模糊、不知道专业术语时

**技术实现**：
```python
async def vector_search(question: str, vehicle_model: str, user_role: str, top_k: int = 20):
    query_embedding = embedding_model.encode(question)
    
    pipeline = [
        {
            "$vectorSearch": {
                "index": "knowledge_chunks_vector_index",
                "path": "embedding",
                "queryVector": query_embedding.tolist(),
                "numCandidates": 100,
                "limit": top_k,
                "filter": {
                    "metadata.vehicle_model": vehicle_model,
                    "state": "active",
                    "metadata.visible_roles": user_role
                }
            }
        },
        {
            "$project": {
                "_id": 1,
                "chunk_text": 1,
                "metadata": 1,
                "score": {"$meta": "vectorSearchScore"}
            }
        }
    ]
    
    results = await db.knowledge_chunks.aggregate(pipeline).to_list(top_k)
    return results
```

**示例**：用户问"车子突然没劲" → 向量检索命中"动力系统故障"相关 Chunk

---

### 路由2：关键词精确检索（BM25）

**适用场景**：故障码、车型编号、部件名称、政策编号等精确术语

**技术实现**：
```python
from rank_bm25 import BM25Okapi

def keyword_search(question: str, vehicle_model: str, top_k: int = 10):
    # 从问题中提取关键词
    keywords = extract_keywords(question)
    
    # 在已加载的 BM25 索引中检索
    scores = bm25_index.get_scores(keywords)
    top_indices = scores.argsort()[-top_k:][::-1]
    
    return [chunks_cache[i] for i in top_indices if scores[i] > 0]
```

**示例**：用户问"P0A0F是什么意思" → 关键词检索精确命中包含"P0A0F"的故障码说明 Chunk

---

### 路由3：结构化业务查询

**适用场景**：需要结合个人车辆数据的问题（是否超保、是否需要保养）

**技术实现**：
```python
async def structured_query(question: str, vehicle_id: str):
    results = []
    
    # 查询车辆档案
    vehicle = await db.vehicles.find_one({"_id": vehicle_id})
    if vehicle:
        results.append({
            "type": "vehicle_info",
            "content": format_vehicle_info(vehicle)
        })
    
    # 查询保养记录
    maintenance = await db.maintenance_records.find(
        {"vehicle_id": vehicle_id}
    ).sort("maintenance_date", -1).limit(3).to_list(3)
    
    # 查询质保规则
    warranty = await db.warranty_policies.find_one({
        "vehicle_model": vehicle["vehicle_model"],
        "state": "active"
    })
    
    return results
```

**示例**：用户问"我的车还在保吗" → 查询车辆档案 + 保养记录 + 质保规则三张集合

---

### 路由4：历史案例检索

**适用场景**：用户想了解类似问题处理经验

**技术实现**：向量相似度检索 typical_cases 集合，按故障现象语义相似度匹配

**示例**：用户问"有没有类似无法启动的案例" → 检索典型案例集合中"故障现象=无法启动"相关案例

---

## 结果融合与打分

```python
def merge_results(vector_results, keyword_results, structured_results, case_results):
    score_map = {}
    
    for item in vector_results:
        chunk_id = str(item["_id"])
        score_map[chunk_id] = {
            "item": item,
            "vector_score": item.get("score", 0),
            "keyword_score": 0,
            "case_score": 0
        }
    
    for item in keyword_results:
        chunk_id = str(item["_id"])
        if chunk_id in score_map:
            score_map[chunk_id]["keyword_score"] = item.get("bm25_score", 0)
        else:
            score_map[chunk_id] = {
                "item": item,
                "vector_score": 0,
                "keyword_score": item.get("bm25_score", 0),
                "case_score": 0
            }
    
    # 加权融合（RRF 算法）
    for chunk_id, scores in score_map.items():
        scores["final_score"] = (
            0.6 * scores["vector_score"] +
            0.3 * scores["keyword_score"] +
            0.1 * scores["case_score"]
        )
    
    # 排序
    sorted_results = sorted(
        score_map.values(),
        key=lambda x: x["final_score"],
        reverse=True
    )
    
    return [r["item"] for r in sorted_results]
```

---

## 关键节点说明（与三层调用链对齐）

### R-QA 路由层 — `app/api/routers/chat.py`
- 负责接收 HTTP 请求 / 解析参数 / 鉴权 / 注入依赖 / 包装响应
- 自身不持有业务状态，所有状态/历史/审计由 P-QA 页面层托管
- 仅做"翻译"：HTTP 协议 ↔ Python 函数调用

### P-QA 页面层 — `app/process/query/page/query_page.py`
- 接收路由层 Pydantic Schema
- 加载用户身份 / 会话历史 / 租户信息
- 调用 G-QA 图编排层 `query_graph_app.invoke(...)`
- 把图返回的状态写回 MongoDB 历史 / 审计 / 可观测指标

### G-QA 图编排层 — `app/process/query/agent/main_graph.py`（StateGraph）
- 负责把"问题预处理 → 意图识别 → 实体抽取 → 四路检索 → 合并 → 重排 → 置信度判断"组装为 LangGraph
- 每个图节点（`node_*`）落到 `app/rag/query/*.py` 的服务
- 通过共享 `QueryState`（TypedDict）跨节点传参
- 状态判定（"进 RAG 生成" / "进低置信度追问"）由图条件边决定

> 详见 `flows/02_RAG检索流程/02_RAG生成回答流程.md` 与 `docs/07_后端落地文档_app目录与运行指南.md`。

---

*流程版本：v1.1 | 更新时间：2026-06-12*
